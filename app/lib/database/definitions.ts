export type Song = {
    id: string;
    title: string;
    file_path: string;
};
  
export type Artist = {
    id: string;
    name: string;
};
  
export type Tag = {
    id: string;
    name: string;
};